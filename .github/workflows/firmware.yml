name: firmware

on:
  push:
    branches: [main]
    tags: ['v*']
    paths:
      - 'drone-client/**'
      - '.github/workflows/firmware.yml'
  pull_request:
    paths:
      - 'drone-client/**'
  workflow_dispatch:
    inputs:
      board:
        description: 'Board to build (defaults to all in matrix)'
        required: false
        default: ''

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        board:
          - ssc338q_skypulse
          - ssc30kq_skypulse

    steps:
      - uses: actions/checkout@v4

      - name: Skip non-selected board (manual dispatch)
        if: github.event_name == 'workflow_dispatch' && inputs.board != '' && inputs.board != matrix.board
        run: echo "skipping" && exit 0

      - name: Install buildroot deps
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            automake autotools-dev bc build-essential cpio curl file git \
            libncurses-dev libtool lzop make rsync unzip wget libssl-dev \
            ca-certificates python3 bison flex gawk perl xxd \
            u-boot-tools device-tree-compiler whiptail

      # libffi's configure script breaks on deeply-nested absolute paths
      # (the default workspace /home/runner/work/<repo>/<repo>/... is too deep).
      # Use a short path for build state.
      - name: Set up short build path
        run: |
          sudo mkdir -p /b
          sudo chown -R $USER:$USER /b

      # Cache the OpenIPC clone so we don't re-clone 60MB each run.
      - name: Cache OpenIPC clone
        uses: actions/cache@v4
        with:
          path: /b/openipc
          key: openipc-firmware-${{ hashFiles('.github/workflows/firmware.yml') }}

      # Cache buildroot output (toolchain + downloads). First build ~45min;
      # cached ~5–15min.
      - name: Cache buildroot output (toolchain, downloads, build state)
        uses: actions/cache@v4
        with:
          path: |
            /b/output/buildroot-2024.02.10
            /b/output/per-package
            /b/output/build
            /b/output/host
            /b/output/dl
          key: buildroot-${{ matrix.board }}-${{ hashFiles('drone-client/drop/br-ext-chip-sigmastar/configs/*_defconfig') }}
          restore-keys: |
            buildroot-${{ matrix.board }}-

      - name: Build firmware (BOARD=${{ matrix.board }})
        working-directory: drone-client/drop
        env:
          JOBS: 4
        run: |
          make BOARD=${{ matrix.board }} \
               OPENIPC=/b/openipc \
               TARGET=/b/output \
               SKYPULSE_SRC="$GITHUB_WORKSPACE/drone-client"

      - name: List artefacts
        if: always()
        run: |
          ls -lh /b/output/images/ 2>/dev/null || true

      - name: Upload firmware archive
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ matrix.board }}
          path: |
            /b/output/images/openipc.*-fpv.tgz
            /b/output/images/rootfs.*
            /b/output/images/uImage.*
          if-no-files-found: warn
          retention-days: 30

      - name: Upload build log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log-${{ matrix.board }}
          path: /b/output/build/*/.stamp_*
          if-no-files-found: ignore
          retention-days: 7

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artefacts
          pattern: firmware-*
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artefacts/openipc.*-fpv.tgz
          generate_release_notes: true
